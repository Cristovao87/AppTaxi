@model IEnumerable<AppTaxi.Models.Rota>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome_Rota)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome_Rota)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RotaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RotaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RotaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
