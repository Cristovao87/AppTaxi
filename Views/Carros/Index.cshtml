@model IEnumerable<AppTaxi.Models.Carro>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Matricula)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Motorista)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cobrador)
            </th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Matricula)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Motorista.MotoristaID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cobrador.CobradorID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CarroID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CarroID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CarroID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
